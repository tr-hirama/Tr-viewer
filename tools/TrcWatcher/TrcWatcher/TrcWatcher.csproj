<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>TrcWatcher</RootNamespace>
    <AssemblyName>TrcWatcher</AssemblyName>
    <ApplicationIcon>appicon.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <None Update="appicon.ico" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!--
    Shift-JIS(CP932) は CodePagesEncodingProvider 経由で利用する。
    net10.0-windows ではフレームワークに含まれるため追加パッケージは不要。
  -->

</Project>
