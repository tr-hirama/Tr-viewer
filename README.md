# DXF ビューワー

ブラウザだけで動く、シンプルな DXF ファイルビューワーです。
ビルド不要・サーバー不要・オフライン動作。`index.html` をブラウザで開くだけで使えます。

## 使い方

1. `index.html` をダブルクリックしてブラウザで開く
2. DXF ファイルを画面に **ドラッグ&ドロップ**、または「ファイルを開く」から選択

### 操作

| 操作 | 動作 |
| --- | --- |
| マウスホイール | マウス位置を中心に拡大／縮小 |
| ドラッグ | 移動（パン） |
| ダブルクリック | 全体表示にリセット |

`sample.dxf` を読み込むと動作を確認できます。

## 対応エンティティ

`LINE` / `LWPOLYLINE` / `POLYLINE`（旧形式 VERTEX） / `CIRCLE` / `ARC` / `POINT` / `TEXT` / `MTEXT`

- ASCII 形式の DXF に対応しています（バイナリ DXF は未対応）。
- Y 軸の上下反転、円弧の角度方向（DXF は反時計回り）を考慮して描画します。
- 未対応のエンティティは読み飛ばします。

## 今後の拡張候補

- レイヤーごとの色分け
- `ELLIPSE` / `SPLINE` / `HATCH` / `DIMENSION` 対応
- `INSERT`（ブロック参照）の展開
- 座標表示・グリッド

## ライセンス

MIT
